import React from 'react';
import { SearchBar } from '../../components';

const LandingPage = () => {
    return (
        <div>
            <SearchBar />
        </div>
    );
};

export default LandingPage;
